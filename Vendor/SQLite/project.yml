fileGroups:
- Vendor/SQLite
targets:
  SQLite:
    type: framework
    platform: iOS
    sources: 
    - Vendor/SQLite/Sources
    settings:
      INFOPLIST_FILE: Vendor/SQLite/Support/Info.plist
      IPHONEOS_DEPLOYMENT_TARGET: 11.0
    scheme:
      testTargets:
      - SQLiteTests
  
  SQLiteTests:
    type: bundle.unit-test
    platform: iOS
    sources: 
    - Vendor/SQLite/Tests
    settings:
      INFOPLIST_FILE: Vendor/SQLite/Support/Tests-Info.plist
      IPHONEOS_DEPLOYMENT_TARGET: 11.0
    dependencies:
    - target: SQLite
